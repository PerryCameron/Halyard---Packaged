(fixed) blank invoice tab that pops up when you put in a new invoice
(fixed) Memtype to change on record when you change it in history table view
(fixed) Make Jotform work
(fixed) Datepicker not functioning correctly
(fixed) Secondary and Dependent 1 both displaying as secondary
(fixed) When making new people it puts a null in nickname
SQLUPDATE uses 7 or so methods, for people when it should use 1

features
Make is so it remembers last server visited

For some reason work credits got out of whack with John Turner in 2015. It may have been user error.

When making deposit report, it prints out current year instead of the selected year, see 2015 Deposit #2

When adding a Note you can't add a single quote. need to fix this.

When adding a fee, need to refresh the fee list or add the fee


Image mainIcon = new Image(Objects.requireNonNull(getClass().getResourceAsStream("/icon_24.png")));
		this.getIcons().add(mainIcon);

EmailDTO emailDTO = email.get(selectedIndex);

